<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xlbs.apiservice.dao.imp.RoleDao">

    <sql id="entity">
        created_by as createdBy,
        created_date as createdDate,
        last_modify_by as lastModifyBy,
        last_modify_date as lastModifyDate
    </sql>

    <sql id="roleField">
        id as roleId,
        `name` AS `name`,
        <include refid="entity"/>
    </sql>

    <!--查询所有角色信息-->
    <select id="findAllRole" resultType="com.xlbs.apiservice.entity.Role">
        select
          <include refid="roleField"/>
        from
          t_role
    </select>

    <!--通过userId查询用户信息-->
    <select id="findRolesByUserId" resultType="com.xlbs.apiservice.entity.Role">
        select
          user_id as userId,
          role_id as roleId,
          <include refid="entity"/>
        from
          t_user_role
        where
          user_id = #{userId}
    </select>

    <!--批量保存用户角色信息-->
    <insert id="saveUserRoles">
        insert into t_user_role
          (user_id, role_id, created_by, created_date,last_modify_by,last_modify_date)
        values
          <foreach collection ="list" item="role" separator =",">
            (#{role.userId}, #{role.roleId}, #{role.createdBy}, #{role.createdDate}, #{role.lastModifyBy}, #{role.lastModifyDate})
          </foreach >
    </insert>

    <!--删除用户角色信息-->
    <delete id="deleteUserRoles" >
        delete from t_user_role where user_id = #{userId}
    </delete>

</mapper>